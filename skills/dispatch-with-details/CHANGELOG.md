# Changelog

## 0.1.0 (2026-05-22)

Initial release.

- Wrapper over `emploke task dispatch` that takes a brief-file path, auto-derives a 200-char summary for `--brief`, forwards the body via `--details-file`, and returns the parsed task id. PowerShell + bash variants.
